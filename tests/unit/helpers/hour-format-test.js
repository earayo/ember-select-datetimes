import { hourFormat } from 'dummy/helpers/hour-format';
import { module, test } from 'qunit';

module('Unit | Helper | hour format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hourFormat([42]);
  assert.ok(result);
});
